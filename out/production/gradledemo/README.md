# Steps

1. cd into project
```shell
cd logger
```

2. Build `.class` files
```shell
javac org/gradledemo/logger/**/*.java
```

3. Build executable `jar`
```shell
jar cf logger.jar org/gradledemo/logger/**/*.class
```
